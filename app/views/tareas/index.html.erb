
<h1>Listing tareas</h1>

<table>
  <thead>
    <tr>
      <th>Descripcion</th>
      <th>Tiempo limite</th>
      <th>Complejidad</th>
      <th>Puntuacion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tareas.each do |tarea| %>
      <tr>
        <td><%= tarea.descripcion %></td>
        <td><%= tarea.tiempo_limite %></td>
        <td><%= tarea.complejidad %></td>
        <td><%= tarea.puntuacion %></td>
        <td><%= link_to 'Show', tarea %></td>
        <td><%= link_to 'Edit', edit_tarea_path(tarea) %></td>
        <td><%= link_to 'Destroy', tarea, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tarea', new_tarea_path %>
