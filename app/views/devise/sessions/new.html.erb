<div class="authform">
  <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>
    <h3>Ingresar</h3>
    <%= devise_error_messages! %>
    <div class="form-group">
      <%- if devise_mapping.registerable? %>
        <%= link_to 'Registrarse', new_registration_path(resource_name), class: 'right'  %>
      <% end -%>
      <%= f.label 'Correo' %>
      <%= f.email_field :email, :autofocus => true, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%- if devise_mapping.recoverable? %>
        <%= link_to "Olvidó Clave?", new_password_path(resource_name), class: 'right' %>
      <% end -%>
      <%= f.label 'Clave' %>
      <%= f.password_field :password, class: 'form-control' %>
    </div>
      <%= f.submit 'Ingresar', :class => 'button right' %>
      <% if devise_mapping.rememberable? -%>
        <div class="checkbox">
          <%= f.check_box :remember_me %> <%= f.label 'Recuerdame' %>
        </div>
      <% end -%>
  <% end %>
</div>
